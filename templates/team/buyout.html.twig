{% extends 'base.html.twig' %}

{% block title %}{{ 'pages.titles.buyout' | trans }}{% endblock %}

{% block body %}

    <div class="container mt-5"><!-- Container -->



        <div class="col-md-6 offset-md-3"><!-- Form -->
            <div class="card text-white bg-custom-dark mb-3" style="width: 100%;">
                <div class="card-header pt-4 pb-4">{{ 'template.player.buyout.title' | trans({'teamName': app.user.player.team.fullName}) }}</div>
                <div class="card-body">
                    <p>{{ 'template.player.buyout.text' | trans }}</p>

                    {{ form_start(form) }}

                    {{ form_widget(form) }}

                    {{ form_end(form) }}
                </div>
            </div>
        </div><!-- Form -->



    </div><!-- Container -->

{% endblock %}
